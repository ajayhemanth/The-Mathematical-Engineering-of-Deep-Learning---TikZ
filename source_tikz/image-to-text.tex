\documentclass[border=1cm]{standalone}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.misc}
\usetikzlibrary{cd, fit, calc}
\usetikzlibrary{positioning}
\usetikzlibrary{decorations.markings}
\usepackage{medl_colors}
\usepackage{graphicx} %package to manage images
\graphicspath{ {./images/} } 

\usepackage{arrayjob}
\def\words{{
{"Three","good","friends", "having", "fun"}
}}

\begin{document}
\begin{tikzpicture}

\foreach \i in {1,...,5}
{
    \pgfmathparse{int(\i-1)}\edef\j{\pgfmathresult};
    \pgfmathparse{\words[0][\j]} \edef\word{\pgfmathresult};
    \node[draw, rounded corners, minimum width=.5cm, minimum height=2cm, fill=blue1] (unit\i) at (\j*2, 0) { };
    \node[rotate=90, below of=unit\i, node distance=0cm] {unit};
    \node[above of=unit\i, node distance= 2cm] (word\i) {\texttt{\word}};
    \draw[-Triangle] (unit\i) -- (word\i);

    \ifnum\i>1
        \draw[-Triangle] (unit\j) -- (unit\i);
        \draw[-Triangle,thick, bend right, looseness=1.5] (word\j) to[out=90, in=-90] (unit\i.south);
    \fi
}

\node[trapezium, align=center, draw, trapezium angle=80, minimum width = 2cm, shape border rotate=270, left of = unit1, node distance = 3cm ] (t1) { CNN };  

\node[scale = .07, left of = t1, node distance = 80cm] (inputpic)  {\includegraphics {images/threepersons.jpeg}};
\node[below of=unit1, node distance= 2cm] (word) {\texttt{<Start>}};

\draw[-Triangle] (inputpic) -- (t1.west);
\draw[-Triangle] (t1.east) -- (unit1);
\draw[-Triangle] (word) -- (unit1);



\end{tikzpicture}
\end{document}
